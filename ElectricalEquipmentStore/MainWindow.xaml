<Window x:Class="ElectricalEquipmentStore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ElectricalEquipmentStore"
        mc:Ignorable="d"
        Title="ЭлектроМир - Магазин электрооборудования" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#F5F9FF">

    <Window.Resources>
        <!-- Стили -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#4A90E2"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#357AE8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2A65C4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BackButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <Grid>
                                <Path Data="M15.41,16.58L10.83,12L15.41,7.41L14,6L8,12L14,18L15.41,16.58Z" 
                                      Fill="#4A90E2" 
                                      Stretch="Uniform"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Основной контейнер -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Шапка -->
                <RowDefinition Height="*"/>
                <!-- Основное содержимое -->
                <RowDefinition Height="Auto"/>
                <!-- Футер -->
            </Grid.RowDefinitions>

            <!-- ШАПКА -->
            <Border Grid.Row="0" 
                    Background="White" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="0,0,0,1"
                    Padding="20,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Логотип и название -->
                    <StackPanel Grid.Column="0" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center">
                        <Button x:Name="BackButton" 
                                Style="{StaticResource BackButtonStyle}"
                                Click="BackButton_Click"
                                Visibility="Collapsed"/>

                        <TextBlock Text="⚡ ЭлектроМир" 
                                   FontSize="22" 
                                   FontWeight="Bold" 
                                   Foreground="#4A90E2"
                                   VerticalAlignment="Center"
                                   Margin="10,0"/>
                    </StackPanel>

                    <!-- Навигация -->
                    <StackPanel Grid.Column="1" 
                                Orientation="Horizontal" 
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">
                        <!-- Общие кнопки (видны всем) -->
                        <Button x:Name="CatalogButton" 
                                Content="Каталог" 
                                Style="{StaticResource NavButtonStyle}"
                                Click="CatalogButton_Click"/>

                        <Button x:Name="CartButton" 
                                Content="Корзина" 
                                Style="{StaticResource NavButtonStyle}"
                                Click="CartButton_Click"/>

                        <Button x:Name="OrdersButton" 
                                Content="Мои заказы" 
                                Style="{StaticResource NavButtonStyle}"
                                Click="OrdersButton_Click"/>

                        <!-- Для сотрудников -->
                        <Button x:Name="OrderManagementButton" 
                                Content="Управление заказами" 
                                Style="{StaticResource NavButtonStyle}"
                                Visibility="Collapsed"
                                Click="OrderManagementButton_Click"/>

                        <Button x:Name="SuppliesButton" 
                                Content="Поставки" 
                                Style="{StaticResource NavButtonStyle}"
                                Visibility="Collapsed"
                                Click="SuppliesButton_Click"/>

                        <!-- Для администраторов -->
                        <Button x:Name="AdminPanelButton" 
                                Content="Панель администратора" 
                                Style="{StaticResource NavButtonStyle}"
                                Visibility="Collapsed"
                                Click="AdminPanelButton_Click"/>
                    </StackPanel>

                    <!-- Информация о пользователе -->
                    <StackPanel Grid.Column="2" 
                                Orientation="Horizontal" 
                                VerticalAlignment="Center">
                        <TextBlock x:Name="UserNameText" 
                                   Text="Пользователь" 
                                   Style="{StaticResource HeaderTextStyle}"
                                   Margin="0,0,15,0"/>

                        <Button x:Name="LogoutButton" 
                                Content="Выйти" 
                                Style="{StaticResource NavButtonStyle}"
                                Background="#E74C3C"
                                Click="LogoutButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ОСНОВНОЕ СОДЕРЖИМОЕ -->
            <Frame x:Name="MainContentFrame" 
                   Grid.Row="1" 
                   NavigationUIVisibility="Hidden"
                   Navigated="MainContentFrame_Navigated"/>

            <!-- ФУТЕР -->
            <Border Grid.Row="2" 
                    Background="#2C3E50" 
                    Padding="20,10">
                <TextBlock Text="© 2024 ЭлектроМир. Магазин электрооборудования. Все права защищены." 
                           Foreground="White" 
                           HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Grid>
</Window>